.btn-group(aria-label="Basic example" role="group")
  - if can?(:read, Article)
    = link_to t(:show), show_p, class: 'btn btn-default'
  - if can?(:update, element)
    = link_to t(:edit), edit_p, class: 'btn btn-default'
  -if can?(:update, element)
    = link_to t(:destroy), destroy_p, method: :delete, class: 'btn btn-default'
