.col-sm-8.center-block(style='margin-top: 120px')
  .panel.panel-default
    .panel-heading= t(:task)

    .panel-body
      %p= @customer.tasks.first.fio
      %p= @customer.tasks.first.email
      %p= @customer.tasks.first.phone
      %p= @customer.tasks.first.link
